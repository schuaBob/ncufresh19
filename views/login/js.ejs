<script>
    var url = new URL(window.location.href);
    var id = $("input[name=id]").val(url.searchParams.get('id'));
    document.getElementById("id").defaultValue = id;

    $("#student_id").focus();

    function login(){
        var url = new URL(window.location.href);
        $("input[name=id]").val(url.searchParams.get('id'));
    }
</script>